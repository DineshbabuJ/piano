name: paino
description: A sample Flutter application
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  audioplayers: ^4.1.0
  # Add the web_audio dependency here

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/piano_notes/